# Provenance

This directory is reserved for sanitized publication-reproduction manifests.
The final manifest is generated by:

```bash
python scripts/run_complete_climate_workflow.py
```

The sanitized manifest contains file names, sizes and SHA-256 checksums, but no
absolute local paths. The full local manifest is written to `outputs/provenance/`
and is not committed.
